//this is a default fallback page for the complex dashboard. it is needed for a default view when the browser reloads and cant find the present state of the slots.

const DefaultComplexDashboardPage = () => {
  return <div>Complex dashboard</div>;
};

export default DefaultComplexDashboardPage;
