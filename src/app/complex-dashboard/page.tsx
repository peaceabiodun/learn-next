const ComplexDashboardPage = () => {
  return <div>Complex dashboard page 
    <p>Use paralle routes for dashborads and pages that have multiple components in them</p>
  </div>;
};

export default ComplexDashboardPage;
